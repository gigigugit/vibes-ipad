What I want is for you to build a new function for the Selector overlay that does the following:
1. The selector overlay will now function as a "window" overlaid on the page, but with additional ability to be moved around by the user, minimized, maximized, and closed.
2. It will continue to have the current function of allowing users to select elements on the page to serve as selectors for variable data to be parsed.
3. new function will be to add variables, which will be defined in the display by the user, and these will be used to store the data that is parsed from the selected elements on the page.
4. The user will be able to create, edit, and delete these variables within the selector overlay interface.
5. The use of this interface for new variables will be the same as for the existing variable selector.
6. We will add a second panel that expands to allow for template editing and creation, with the ability to save and manage templates. 
7. I want to add a general "parser" that will allow the user to define sets of possible data to be grabbed (as a list of possible options for the variable) and also to enable secondary processing of the variable data such as appending different strings, trimming portions, or applying if/then/elst logic to the different options for the variable data. This will allow for more complex data manipulation and formatting before it is used in templates or other outputs.
8. The parser will also include the ability to set up rules for how the data should be processed based on certain conditions, such as the presence of specific keywords or patterns in the data.
